import { FilterElementsPipe } from './filter-elements.pipe';

describe('FilterElementsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterElementsPipe();
    expect(pipe).toBeTruthy();
  });
});
